# Core
redis==5.1.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0,<4.10.0

# ML Frameworks
# Pinned to versions with cu118 wheels — must match the container's CUDA 11.8
# runtime, otherwise GroundingDINO's CUDAExtension build fails (torch's
# bundled CUDA libs and nvcc headers diverge).
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.4.1
torchvision==0.19.1
ultralytics>=8.0.0
timm>=0.9.0

# Pose Estimation (RTMPose)
rtmlib>=0.0.10
onnxruntime-gpu==1.17.1
scipy>=1.10.0

# Utilities
Pillow>=10.0.0
pyyaml>=6.0
